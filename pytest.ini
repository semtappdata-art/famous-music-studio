[pytest]
# tests/ altındaki modüller repo kökündeki auto_process.py/config.py/git_sync.py
# vb.'yi import ediyor — bu, `python -m pytest` DIŞINDA (ör. CI'daki gibi düz
# `pytest` konsol script'i) çalıştırıldığında repo kökü sys.path'e otomatik
# eklenmiyor. `pythonpath = .` bunu invocation şeklinden bağımsız garanti eder.
pythonpath = .
